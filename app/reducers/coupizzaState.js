import { combineReducers } from 'redux';

import {
  GET_ZIP,
  GET_LOCATION, 
  GET_CODES, 
  LOAD_STATUS
} from '../actions/actions.js' 


/*
function coupizzaState(state = initialState, action){
  switch(action.type){
    case: "GET_ZIP" 
      return state.concat()
    case: "GET_LOCATION"
    case: "GET_CODES"
    case: "LOAD_STATUS"
    
    default: 
  }
}

export default combineReducers({

})
*/
